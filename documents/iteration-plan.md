# Singo — 결정 로그 (Iteration Plan)

> ⚠️ **이 문서는 상태를 추적하지 않는다.** 다음 스토리·진행 상태·점수·소유는 **Tracker Boot가 진실의 원천**이다. 여기에 상태를 적으면 트래커와 이중화돼 매 Accept마다 어긋난다 — '이터레이션 플랜'이라는 이름이 흔히 빠지는 함정이 그것이다.
>
> **이 문서가 담는 것은 "왜"다.** 트래커에 안 남는 결정의 근거·맥락. 새 세션이 "왜 이 순서인가 / 무엇을 결정·기각했나"를 빠르게 복원하라고 둔다. "어디까지 했나"는 항상 트래커에서 잡는다(트랙 파일 완료목록도 stale할 수 있다).

**형식**: 시간 역순으로 "핵심 결정" 블록을 쌓는다. 각 블록 = 무엇에서 출발 → 무엇을 캐물음 → 무엇을 결정/기각 → 근거 → **다음 후속**.

> **최신 엔트리가 곧 핸드오프다.** 각 블록 끝에 **"다음 후속"** 포인터(후보 스토리 ID 1~3개 + 한 줄 이유)를 남긴다. 새 PM 세션은 `PM 시작` 시 이 **최신 엔트리의 "다음 후속" + current iteration**만 보고 다음 스토리를 잡는다.

---

## 핵심 결정 (2026-07-15) — MVP 스코프 및 기술 선택

- **배경**: D&F에서 신유진 페르소나(다국적 팀 개인 기여자)를 기준으로 Problem Priority를 "실시간 자동 번역" + "선별적 실시간 알림"으로 좁혔다.
- **결정**:
  - 번역 엔진: **Claude API** (한/영/일 업무 대화체·스레드 맥락 처리에 LLM 기반이 전통 번역엔진보다 우수하다고 판단)
  - 알림 채널: **Slack DM** (별도 푸시/이메일 인프라 없이 Slack Bot API로 구현)
  - 배포: 단일 Netlify 사이트(singo-lingo), production 브랜치=프로덕션, main 브랜치=acceptance 미러
- **기각**: DeepL·Google Translate API — 아시아 언어(한/일) 뉘앙스·문맥 처리에서 LLM 기반보다 약하다고 판단해 기각.
- **근거**: 사용자(지선) 확인 및 웹 검색 기반 번역 API 비교(DeepL/Google/OpenAI 가격·품질, 2026년 기준).
- **다음 후속**: #200028978(Slack 워크스페이스 연결) — 첫 스토리. 이후 순서 권장: #200028978 → #200028990(채널 선택적 구독) → #200028991(Slack 메시지 수신 인프라, Chore) → #200028980(실시간 자동 번역) → #200028992(원문+번역 나란히 보기) → 나머지.

## 핵심 결정 (2026-07-15) — Pre-IPM 리뷰 및 IPM 분리

- **배경**: Tobi가 Pre-IPM에서 5개 스토리의 Gherkin 구조를 리뷰. 이후 IPM에서 Estimate 진행 중 #200028978(5pt)과 #200028980(8pt)이 INVEST "Small" 기준(한 이터레이션 내 완료)을 넘는 것으로 판단.
- **결정**: 두 스토리를 다음과 같이 분리했다.
  - "Slack 연결 & 채널 선택"(5pt) → "Slack 워크스페이스 연결"(2pt) + "채널 선택적 구독"(2pt)
  - "실시간 피드 확인"(8pt) → "Slack 메시지 수신 인프라"(Chore, 3pt) + "실시간 자동 번역"(3pt) + "원문+번역 나란히 보기"(2pt)
- **기각**: 5·8pt 그대로 진행 — 한 이터레이션 내 완료가 어렵고 리뷰·롤백 단위가 커져 기각.
- **근거**: aabt-workflow §4 INVEST 기준 + 인프라(웹훅)와 기능(번역)을 분리해 "신규 e2e인데 인프라가 없으면 별도 Chore로 분리" 원칙 적용.
- **다음 후속**: #200028978(Slack 워크스페이스 연결) — 개발 시작 시 첫 스토리로 할당.
